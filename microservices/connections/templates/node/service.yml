apiVersion: v1
kind: Service
metadata:
  labels:
    service: node
  name: node
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: 0.0.0.0/0
spec:
  ports:
  - name: "8000"
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    service: node
  type: LoadBalancer
